name: schedule

on:
  schedule:
    - cron: "0 0 * * *"
jobs:
  run-script:
     runs-on: ubuntu-latest  

     steps:
       - name: print message
         run: echo "the schedule workflow Executed"
